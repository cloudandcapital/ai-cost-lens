# Third-party notices

## gpt-tokenizer 4.0.0

AI Cost Lens bundles the `o200k_base` encoder from [gpt-tokenizer](https://github.com/niieani/gpt-tokenizer) for local OpenAI raw-text token counting. The package is licensed under the MIT License.

The production build includes `GPT-TOKENIZER-LICENSE.txt` beside the bundled tokenizer. AI Cost Lens uses only the raw-text token count; chat framing, tools, images, audio, and provider-added wrapper tokens remain excluded and are disclosed in the calculator.

## PDF.js 6.3.289

AI Cost Lens uses [PDF.js](https://github.com/mozilla/pdf.js) for local extraction from supported text-based invoice PDFs. Its license is copied into the production vendor directory as `PDFJS-LICENSE.txt`.
